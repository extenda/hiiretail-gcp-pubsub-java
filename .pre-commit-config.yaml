default_stages: [commit]

repos:
  - repo: https://github.com/extenda/pre-commit-hooks
    rev: v0.9.0
    hooks:
      - id: google-java-formatter
      - id: commitlint
        stages: [commit-msg]
